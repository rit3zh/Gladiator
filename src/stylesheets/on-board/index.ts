import { StyleSheet } from "react-native";

export const onBoardingStyles = StyleSheet.create({
  container: {
    width: 200,
    height: 250,
  },

  logo: {
    width: 400,
    height: 200,
  },
});
