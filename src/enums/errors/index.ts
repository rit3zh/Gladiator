export enum Error {
  InvalidCredentials = "auth/invalid-credential",
  AlreadyExists = "auth/email-already-exists",
  InvalidPassword = "auth/invalid-password",
  InvalidEmail = "auth/invalid-email",
  NotFound = "auth/user-not-found",
}
